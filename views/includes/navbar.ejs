<style>
  .navbar {
    margin: 0 auto;
  }

  .logout {
    padding: 0.5rem;
    background-color: #ff7171 !important;
    border-radius: 1rem;
    color: white !important;
  }
  .logout:hover{
    opacity: 0.9;
  }

  .submition {
    border: 1px solid #ff7171;
  }
  .submition:hover {
    background-color: #ff7171 !important;
    border: none;
  }
</style>

<nav class="navbar navbar-expand-md border-bottom sticky-top px-md-4 px-0">
  <div class="container-fluid bg-white">
    <a class="navbar-brand" href="/VerifiedVilla"><i class="fa-brands fa-slack"></i>VerifiedVilla</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav ms-auto">
        <form class="d-flex mt-3 mt-md-0">
          <input class="form-control me-1" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-danger submition " type="submit">Search</button>
        </form>
      </div>
      <div class="navbar-nav ms-auto mt-3 mt-md-0">
        <a class="nav-link" href="/VerifiedVilla/create">Add New Listing</a>
        <% if(!curUser) { %>
          <span class="login mt-3 mt-md-0">
            <a class="nav-link text-white" href="/signup"><b>Sign-Up</b></a>
            <a class="nav-link text-white" href="/login"><b>Login</b></a>
          </span>
          <% } %>
            <% if(curUser) { %>
              <a class="nav-link logout mt-3 mb-3 mb-md-0 mt-md-0" href="/logout"><b>Logout</b></a>
              <% } %>
      </div>
    </div>
  </div>
</nav>